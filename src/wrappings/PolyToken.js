// @flow

/**
 * PolyToken
 */
class PolyToken {
    constructor() {}

  /**
   * Checks the Poly balance of an ethereum account. Returns a BigNumber promise of the users PolyBalance.
   * 
   * @param ethAddress Ethereum address to query the POLY balance of
   */
  checkPOLYBalance(ethAddress: string) {}
}

export type PolyTokenObject = PolyToken;

module.exports = PolyToken;

